{* this message receives support after contact created a new ticket *}
<p>Byl vytvořen nový tiket <strong>{$ticket->getNumber()}</strong> od <strong>{$ticket->getProject()->getHost()}</strong>.</p>
<p>Odpovědět můžete zde: <a href="{$ticket_url}">{$ticket_url}</a></p>

{if $message = $ticket->getLastMessage()}
	{$message->getMessage()}
{/if}