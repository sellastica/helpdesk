{* this message receives support after contact created a new ticket *}

{layout '../@layout_email.latte'}

{block content}
	<p>Byl vytvořen nový tiket <strong>{$ticket->getNumber()}</strong> od
		<strong>{$ticket->getProject()->getHost()}</strong>.</p>
	<p>Odpovědět můžete zde: <a href="{$ticket_url}">{$ticket_url}</a></p>

	<blockquote>
		{$message->getMessage()|noescape}
	</blockquote>
{/block}